from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A few transformer-based models for chemical reactions predictions.',
    author='Jocelyn Lu',
    license='MIT',
)
